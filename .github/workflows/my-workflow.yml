on:
  # pull_request: {}
  push: {}
  workflow_dispatch: {}



jobs:
  my-job:
    runs-on: ubuntu-22.04
    if: ${{true}}
    steps:
      - name: my-step
        run: |
          ls
          echo ${{ github.run_attempt }}
          echo ${{ github.run_number }}
          echo ${{ github.event.pull_request.number }}
          echo ${{ github.ref_name }}
          echo ${{ github.sha }}
          echo ${{ toJson(github.ref) }}

          gh api graphql -f query='{
            repository(owner: "wild-nyankoman",name: "my-inspection") {
              ref(qualifiedName: "${{github.ref}}") {
                target {
                  oid
                }
              }
            }
          }'
        env:
          GH_TOKEN: ${{ github.token }}
    outputs:
      hoge: ${{vars.UNKO}}
  my-job2:
    uses: ./.github/workflows/callee.yml
    secrets: inherit
  xxxxxxx:
    name: XXxxxxxx
    timeout-minutes: 1
    needs: 
      - my-job2
      - my-job
    runs-on: ubuntu-22.04
    env:
      b64: ${{needs.my-job2.outputs.b64}}
    steps:
      - run: |
          echo $b64
          echo "$b64" | base64 -d
